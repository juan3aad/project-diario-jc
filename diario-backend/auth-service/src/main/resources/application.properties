spring.application.name=auth-service

# -------------------------------------------------------------------------------------
# --- Configuracion de Puerto del Microservicio ---
# -------------------------------------------------------------------------------------
server.port=8081

# -------------------------------------------------------------------------------------
# --- Configuracion de Spring Data JPA (PostgreSQL) ---
# Usar una base de datos separada (aunque puede ser el mismo servidor Supabase)
# -------------------------------------------------------------------------------------
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:auth_db}
spring.datasource.username=${DB_USERNAME:jagreda}
spring.datasource.password=${DB_PASSWORD:Estefa2024}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# -------------------------------------------------------------------------------------
# --- Configuracion de JWT (SHARED SECRET) ---
# ESTA CLAVE DEBE SER LA MISMA EN AMBOS SERVICIOS (AUTH Y DIARY)
# Usar una clave Base64 de al menos 32 bytes para HS256
# -------------------------------------------------------------------------------------
jwt.secret.key=TU_CLAVE_SECRETA_LARGA_COMPARTIDA_AQUI_EJEMPLO_BASE64_32_BYTES_0123456789ABCDEF
